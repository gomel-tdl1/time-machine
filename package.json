{
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "fork": "hardhat node",
    "addtime": "hardhat addtime --network localhost",
    "newprice": "hardhat newprice --network localhost",
    "rebalance": "hardhat rebalance --network localhost",
    "state": "hardhat state --network localhost",
    "now": "hardhat now --network localhost",
    "addBalance": "hardhat addBalance --network localhost",
    "addBalanceETH": "hardhat addBalanceETH --network localhost",
    "mine": "hardhat mine --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-network-helpers": "^1.0.8",
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "dotenv": "^10.0.0",
    "ethers": "^5.7.2",
    "hardhat": "^2.7.0",
    "parse-duration": "^1.0.2",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
